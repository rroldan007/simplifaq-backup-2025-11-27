# Test Environment Configuration
NODE_ENV=test
PORT=3002

# Test Database Configuration (PostgreSQL with separate test database)
DATABASE_URL="postgresql://roberto@localhost:5432/simplifaq_test"

# JWT Configuration for tests
JWT_SECRET=test-jwt-secret-key
JWT_EXPIRES_IN=1h
JWT_REFRESH_SECRET=test-refresh-token-secret
JWT_REFRESH_EXPIRES_IN=1d

# Email Configuration for tests (mock)
SMTP_HOST=localhost
SMTP_PORT=587
SMTP_USER=test@example.com
SMTP_PASS=test-password

# Swiss QR Bill Configuration for tests
COMPANY_NAME="Test Company SA"
COMPANY_ADDRESS_LINE1="Test Street 123"
COMPANY_ADDRESS_LINE2="1000 Test City"
COMPANY_IBAN="CH93 0076 2011 6238 5295 7"
COMPANY_VAT_NUMBER="CHE-123.456.789"