import { Router } from 'express';

const router = Router();

// TODO: This entire file is temporarily disabled because the routes are out of sync
// with the CommunicationService, causing the server to crash on startup.
// The service needs to be updated to implement the required methods for campaigns,
// bulk email, statistics, etc., before these routes can be re-enabled.

export default router;